{
  "args": {
    "count": {
      "desc": "Pass the number of results you want per \"page\". Maximum of `100`.",
      "required": false,
      "type": "integer"
    },
    "highlight": {
      "desc": "Pass a value of `true` to enable query highlight markers (see below).",
      "required": false,
      "type": "boolean"
    },
    "page": {
      "desc": "",
      "required": false,
      "type": "integer"
    },
    "query": {
      "desc": "Search query.",
      "required": true,
      "type": "string"
    },
    "sort": {
      "desc": "Return matches sorted by either `score` or `timestamp`.",
      "required": false,
      "type": "string"
    },
    "sort_dir": {
      "desc": "Change sort direction to ascending (`asc`) or descending (`desc`).",
      "required": false,
      "type": "string"
    },
    "token": {
      "desc": "Authentication token. Requires scope: `search:read`",
      "required": true,
      "type": "string"
    }
  },
  "desc": "Searches for messages matching a query.",
  "errors": {}
}